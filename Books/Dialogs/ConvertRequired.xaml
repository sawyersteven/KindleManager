<Controls:MetroWindow x:Class="Books.Dialogs.ConvertRequired"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:icons="http://metro.mahapps.com/winfx/xaml/iconpacks"
        mc:Ignorable="d"
        WindowStartupLocation="CenterOwner"
        Title="Conversion Required"
        SizeToContent="Height"
        Width="375">

    <StackPanel Margin="10">
        <DockPanel>
            <icons:PackIconFontAwesome Kind="ExclamationCircleSolid" DockPanel.Dock="Left" Height="48" Width="48" Margin="5" VerticalAlignment="Center"/>

            <TextBlock Margin="5" TextWrapping="Wrap" DockPanel.Dock="Top">
                <TextBlock Name="textBlockBookTitle"/>
                is incompatibe with Kindle devices and must be converted to mobi before copying to library.
            </TextBlock>
        </DockPanel>

        <Grid Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Click="Cancel" Grid.Column="0">
                Cancel
            </Button>
            <Button Click="Confirm" Grid.Column="2">
                Convert
            </Button>

        </Grid>

    </StackPanel>
</Controls:MetroWindow>
